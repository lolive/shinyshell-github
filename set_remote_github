name_params PACKAGE GITHUB_USER GITHUB_REPO
git config credential.helper store
git --git-dir=$BD/${PACKAGE}/.git remote set-url origin https://${GITHUB_USER}@github.com/${GITHUB_USER}/${GITHUB_REPO}.git
